#converter - denary, binary, octuples , hexadecimal ------NOT COMPLETE!!!!

# -------------------------
# Subprograms
# -------------------------
def den2bin(number):
  binary = ""
  while number>0:
    remainder = number % 2
    binary = str(remainder) + binary
    number = number //2 
  return binary
  
  
def hexadigit():
  if number > 9:
    chr (ord"A"+1)
  
